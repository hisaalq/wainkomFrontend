export interface OrganizerInfo {
    image: string;
    name: string;
    bio: string;
    address: string;
    website: string;
    email: string;
    phone: string;
    rating: number;
    Events: Event[];
}